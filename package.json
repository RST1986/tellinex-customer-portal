{
  "name": "tellinex-customer-portal",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "check:platform-policy": "node scripts/check-platform-policy.mjs",
    "build": "npm run check:platform-policy && vite build",
    "preview": "vite preview",
    "verify": "npm run build"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.0.0",
    "vite": "^5.0.0"
  }
}
